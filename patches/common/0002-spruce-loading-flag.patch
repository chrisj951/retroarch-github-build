diff --git a/retroarch.c b/retroarch.c
index 0000000..0000000 100644
--- a/retroarch.c
+++ b/retroarch.c
@@ -1568,5 +1568,14 @@
       video_st->flags            &= ~VIDEO_FLAG_CACHE_CONTEXT_ACK;
       runloop_st->frame_time_last = 0;
+
+#ifdef HAVE_SPRUCE_IGM
+      /* Signal that the video driver is ready so the spruce
+       * loading animation (running in PyUI) can stop. */
+      {
+         FILE *f = fopen("/tmp/spruce_loading_done", "w");
+         if (f) fclose(f);
+      }
+#endif
    }

    /* Regular display refresh rate startup autoswitch based on content av_info. */
